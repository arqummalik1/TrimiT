# Mobile assets

Required for EAS builds (referenced in `app.config.js`):

- `SquareLogo.png` — app icon / splash
- `adaptive-icon.png` — Android adaptive icon + notification icon
- `sounds/notification.mp3` — Android notification channel sound

Ensure these files are **committed to git** before `eas build` on CI.
