# Web Server & API
fastapi==0.136.1
uvicorn==0.46.0
gunicorn==25.3.0
python-multipart==0.0.27
email-validator==2.3.0
slowapi==0.1.9

# Database & Auth
# NOTE: We use a hand-rolled httpx client (`core/supabase.py`) instead of the
# official `supabase` Python SDK, so the SDK is not listed here. If you ever
# add `from supabase import …` somewhere, re-add `supabase==2.29.0` and pin
# transitive deps deliberately.
python-dotenv==1.0.1
PyJWT==2.12.1
cryptography==47.0.0
cachetools==5.5.2

# Payments

# Utils
pydantic==2.12.5
pydantic-settings==2.14.0
httpx==0.28.1
python-dateutil==2.9.0.post0
tenacity==9.1.4
pillow==12.2.0

# Observability
sentry-sdk==2.58.0
python-json-logger==4.1.0
